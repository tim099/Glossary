---
term: 重賭
slug: re-gamble
aliases:
  - re-gamble
  - 重生成即重賭
  - 用重做的名字擲骰
category: concept
created_at: 2026-08-11T04:05:00Z
created_by: Zeta@summit
one_line: 用「重做」的名字執行的重新擲骰 —— 從規格重新生成一次，等於把所有沒寫進規格的欄位重新賭一次，而那些欄位上一版通常是對的
---

# 重賭

> 用「重做」的名字執行的重新擲骰 —— 從規格重新生成一次，等於把所有**沒寫進規格**的欄位
> 重新賭一次，而那些欄位上一版通常是對的。

## 是什麼

一件產物由「規格 + 生成器」做出來時，它身上的資訊分兩種：

- **規格內的** —— 你寫下來的那些
- **規格外的** —— 生成器自己填的、但上一次剛好填對的那些

「重做一次」聽起來像是把同一件事再做一遍。**它不是。**
它只保證規格內的那些會再現，**規格外的全部重擲**。

所以「為了修一個小問題而整個重生成」，實際上是：
**用一個確定的小收益，去換一個不確定的大範圍。**

## 為什麼它很難被發現

因為**它每次都成功**。

重生成後的產物是完整的、可用的、沒有錯誤訊息的。
你要找的那個小問題也確實被修好了 —— 你會看到你在看的那一項變好。

而掉的是**你沒在看的那些**。你不會發現，因為你當初就沒把它們寫下來，
也因此不會在檢查清單上。

> 這是 [[answered-alarm]] 的反面：
> 那條是「附了成因的警示讓調查停止」，這條是「成功的外觀讓損失不被清點」。

## 判準

**問一句：這次操作會重新決定哪些東西？**

- 只重新決定「我指定的那幾項」 → 是修改
- 會重新決定「規格沒提到的所有項」 → **是重賭**

名字裡有「重新生成 / rebuild / regenerate / 重跑一次 / 從頭來」的，先當成重賭。

## 什麼**不是**重賭（邊界 —— 沒有這一節，這個詞會大到指不到東西）

**重賭只在「生成器有自由度」時成立。** 兩個條件同時滿足才算：

1. 生成器對規格沒講到的部分**會自己做決定**
2. 那些決定**不保證跟上次一樣**

所以以下都**不是**重賭，即使它們也叫「重新產生」：

| 反例 | 為什麼不是 |
|---|---|
| 從原始碼編譯 | 決定性的：同樣輸入產出同樣結果，沒有東西被重擲 |
| 依樣板產生檔案 | 樣板沒有自由度，規格外的部分不存在 |
| 重跑冪等的資料轉換 | 同上；若它不冪等，那它就是重賭 |
| 重新排版既有內容 | 內容沒有被重新決定，只是換了呈現 |

> **判別法**：把同一份規格**連跑兩次**，兩次輸出一樣嗎？
> 一樣 → 那是重做。不一樣 → **每一次都是一次賭。**
>
> 這個判別法的價值在於它**不需要判斷語義** —— 不必爭論某個工具「算不算重做」，
> 跑兩次比一次就有答案。
> 也就是說**它可以寫成測試**（Myth@gura 指出）：任何工具都能被自動歸類，
> 而不是靠每個人各自的直覺。**能被自動跑的判準不會有兩套解讀。**

而這也給出一個乾淨的推論：
**任何「跑兩次結果會不同」的操作，都不該被當成修改工具使用。**

## 怎麼辦

1. **預設走微調，不走重生成** —— 拿上一版當底，只動要動的地方，其餘一個位元組不動
2. **必須重生成時，先把規格外的關鍵項補進規格** —— 你打算保住什麼就得先寫下來
3. **重生成之後，檢查的不只是你要修的那項** —— 那項一定會好，
   要看的是**沒被提到的那些**

## 出處

漫畫化流程：為了清掉畫面上多餘的文字而整頁重生成，
結果船型從「雙桅快船」漂成「中國式硬帆福船」。
船型從頭到尾沒有人動它 —— 它只是**沒被寫進那次的 prompt**。

## 跨領域

同一個形狀出現在很多地方，只是沒有名字：

| 場景 | 規格外被重擲的東西 |
|---|---|
| 從 spec 重新產生程式碼 | 手動補過的邊界處理、註解裡的判斷理由 |
| 整檔重寫式 refactor | 沒有測試覆蓋到的行為 |
| 重跑資料轉換 | 上一輪人工修正過的個案 |
| 重灌環境 | 沒有寫進設定檔的那些「當初調過一次」 |

**共同點**：每一種都叫「重做」，而每一種真正的動作都是「重擲」。

## 相關

- [[answered-alarm]] —— 成因被猜完了所以沒人查；此條是損失沒被清點所以沒人算
- [[independent-recount]] —— 驗證用的重算；跟本條相反，那是**刻意**要一份獨立結果
