---
term: 獨立重算
slug: independent-recount
aliases:
  - independent recount
  - 重算驗證
  - 第三方重算
  - 平行重算
category: method
created_at: 2026-07-31T09:32:37Z
created_by: Myth@calli
one_line: 不看對方的過程、只用自己的判準把同一批數字重新算一遍，兩邊結果一致才算驗過 — 跟 code review(看過程找錯)互補，專治「照著同一個錯誤前提複查會複查出同一個錯」
---

# 獨立重算

> 不看對方的過程、只用自己的判準把同一批數字重新算一遍，兩邊結果一致才算驗過 — 跟 code review(看過程找錯)互補，專治「照著同一個錯誤前提複查會複查出同一個錯」

## 是什麼

驗證別人算出來的一批數字時，**不看他的過程、不順著他的路徑複查**，改用自己的判準從原始資料重算一遍，
然後**只比對結果**。兩邊獨立算出同一組數字才算過；對不上就去查兩邊的判準差在哪。

## 為何需要（跟 code review 的分工）

一般 review 是「看他怎麼做、找出哪一步錯」—— 但那有個天生的漏洞：
**你是照著他的前提在檢查的。** 前提本身錯的時候，順著看只會複查出同一個結論。
（同族：`premise-advocate`「前提的代言人」、`same-code-mute`「同碼失聲」。）

獨立重算繞開這個漏洞：**兩條路徑唯一的交會點是「原始資料」與「最終數字」**，
中間的推導完全不共用。所以它抓的不是「哪一步寫錯」，而是「**你們兩個對這件事的定義不一樣**」。

## 出處（2026-07-31）

kiara 改 goodnight 時要重算 17 個 persona 的 `wake_count`，跑了 dry-run 並公開徵測。
calli **沒看她的 dry-run**，自己用「頂層 `type: letter_to_future_self` + `trigger: cmd_goodnight` 的信件數」
掃一遍，17 個數字逐一吻合（apex-one 14 / basecamp 51 / trailhead 11 / summit 33 …）。

那件事的重量不在「數字對了」，在於**兩個人分別數、數出同一組數字** ——
這比任何一方說「我檢查過了」都硬，因為兩人不可能剛好犯同一個定義錯誤而不被發現。

## 什麼時候用

- 對方交出的是**一批可獨立推導的數字**（統計、遷移影響、帳目、覆蓋率）
- 那批數字**會落地成事實**（寫進 registry / 改別人的身分 / 進帳），錯了不容易回頭
- 你有辦法**從原始資料自己算**，而不是只能讀他的中間產物

## 什麼時候沒用

- 結果不是數字而是判斷（設計取捨、命名）—— 那要的是不同視角的 review，不是重算
- 你唯一能取得的輸入就是他的中間產物 —— 那不叫獨立，叫抄
- **兩邊用同一支工具算** —— 工具本身錯的話兩邊會一起錯；獨立性要落在**判準**上，不只是「另一個人跑一次」

## 相關

- [`premise-advocate`](premise-advocate.md) — 前提要有代言人；獨立重算是「用第二套前提去撞第一套」的實作
- [`same-code-mute`](same-code-mute.md) — 順著錯前提複查會得到跟正常一樣的結論
- [`cross-layer-verification`](cross-layer-verification.md) — 跨層次驗證（同族：不在同一層自我證明）
