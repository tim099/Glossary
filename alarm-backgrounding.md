---
term: 告警背景化
slug: alarm-backgrounding
aliases:
  - 警告背景化
  - alarm backgrounding
  - 黃燈變背景
  - 每次都亮的燈
category: concept
created_at: 2026-08-03T01:51:16Z
created_by: apex-one (Altair)
one_line: 內容為真但每次都出現的 fail-soft 告警被讀者背景化 — 系統一直出聲, 出聲本身變成沉默; 靜默失效的對稱極
---

# 告警背景化

一則**內容完全正確**的告警，因為出現得夠頻繁、位置夠固定，被讀者的注意力自動歸類成版面的一部分 —— 從此它每次都在喊，而每次都沒人聽。

**跟「靜默失效」是對稱的兩極**：靜默失效是系統不出聲；告警背景化是**系統一直出聲，出聲本身變成了沉默**。後者更難抓，因為所有人（包括寫它的人）都能指著那行說「我們有告警啊」。

## 判準（三個都中才算）

1. **內容為真** —— 它報的是真問題，不是誤報。（誤報導致的忽視是另一種病：狼來了）
2. **位置固定且高頻** —— 每次跑同一個指令都會出現，於是視覺上與 header／footer 同層。
3. **fail-soft** —— 它不擋流程。使用者拿得到想要的結果，於是沒有停下來的動機。

## 真實案例（家規：入族必附踩坑）

`tavern_catchup.py:447-464` 的「同事上下線快照 diff」（Tim 2026-08-01 拍板）：呼叫端寫得完整 —— 首次建快照、上下線 diff、append 進 durable inbox，註解甚至明寫 **「fail-soft + 明講，不靜默」**。但 `AgentCommands/_lib/presence_snapshot.py` 從來不存在，全 repo 只有那一個 import 點、零定義。

於是每次 catchup 都印：
`⚠ presence 快照更新失敗（不影響 catchup 主結果）：cannot import name 'presence_snapshot'`

**功能從拍板到被發現，三天，一次都沒跑過。** 而它每一次都吼了。

## 為什麼「不靜默」不夠

設計者預先寫下「不靜默」四個字，是對的直覺、不完整的解法。**不靜默解決的是「有沒有出聲」，解決不了「出聲有沒有被聽見」。**

出聲要被聽見，至少要滿足一項：
- **會痛**（擋流程 / 非零退出碼）
- **會累積**（進 durable 待辦層，而不是滾動的 stdout）
- **會變化**（第 N 次跟第 1 次長得不一樣，例如帶次數：「已連續失敗 47 次」）

第三項成本最低、破壞性最小 —— **同一行字加一個遞增計數，就能讓它從版面裡跳出來。**

## 對照：一個做對了的例子（同日）

同一天跑 `Cmd_Glossary` 註冊本詞條時漏了 `one_line`，它**直接擋下、非零退出、印完整 stack trace**。三十秒內修好。

**同一個 repo 裡，兩種告警設計並存**：一個擋你（三十秒解決），一個提醒你（三天沒人動）。差別不在誰寫得認真，在**有沒有讓忽略它變成不可能**。

## 反面守則

**別用「我們有印警告」當作已經處理過的證據。** 問下一句：「上次有人因為那行字改了行為，是什麼時候？」答不出來 → 那行已經背景化了，等於不存在。

## 相關

- [[appearance-vs-reality-family]] —— 同族：介面層宣稱不可信
- 靜默失效 —— 本詞的對稱極
