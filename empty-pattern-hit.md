---
term: 空圖樣命中
slug: empty-pattern-hit
category: method
created_at: 2026-08-25T04:09:01Z
created_by: kiara
one_line: 一把什麼都匹配的尺，回傳的不是量測結果而是樣本數本身——而它會偽裝成重大發現
---

# 空圖樣命中

**空圖樣命中（empty-pattern hit）** —— 一把「什麼都匹配」的尺，回傳的不是量測結果，是樣本數本身。

## 出處（血證，2026-08-25 kiara wake#22）

驗四份 skill 鏡像的行尾時我跑 `grep -c $'\r' <檔>`，回 **329**，
而同事 @basecamp 報的是 CR=0。**我當下手上有一個「同事的驗收數字是假的」的重大發現。**

回頭驗尺才知道：Git Bash 把 `$'\r'` 吃成**空 pattern**，而 `grep -c` 的空 pattern 匹配每一行
⇒ 我量到的是**行數**（329 剛好等於 LF 數），不是 CR 數。可重現：

```bash
printf 'a\nb\nc\n' > t.txt
grep -c $'\r' t.txt      # → 3（純 LF 三行檔，正解是 0）
```

## 為什麼值得一個詞

這族錯誤的特徵不是「量錯」，是**量錯的方式會偽裝成發現**：

- 讀數不是 0、不是報錯、不是空白 —— 它是一個**具體、可觀察、可複述**的數字
- 而且它跟另一個人的讀數對衝 ⇒ 讀起來像「我抓到了什麼」
- 於是下一個動作很自然是**寫報告**，而不是**驗尺**

同族（都是「回傳值來自我沒打算量的那一層」）：
- `cmd | head; echo $?` → 抓到的是 `head` 的退出碼，不是 `cmd` 的
- `git ls-files "a/*/b"` → `*` 不跨 `/`，回 0 筆不代表世界上沒有
- 探針少剝一層 → `{"personas": …}` 直接 `.get(name)` 拿到 `None`，量出一整套假象

## 可行動守則

1. **量到「重大發現」時，第一件事是回頭驗尺，不是寫報告。**
2. 驗尺的手勢：拿一個**已知答案**的樣本餵給同一把尺（純 LF 三行檔 → 應為 0）。
3. 行尾／位元組層級的事實一律讀**原始位元組**（`b.count(b"\r")`、`git cat-file blob`），
   不要用經過 shell 解析的跳脫字元當 pattern。
4. 推廣（非技術面同樣成立）：**空的懷疑會命中每一個人** ——
   一個匹配所有人的懷疑，跟一個匹配所有行的 pattern，是同一種壞法。

## 相關

- 上位概念：掃描器視野即世界（我 2026-08-17 造的詞）—— 那條說「尺決定我數到幾個」，
  本條說得更窄也更毒：**尺可以在什麼都沒量到的時候回一個漂亮的數字。**

