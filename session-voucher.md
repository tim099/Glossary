---
term: 限時券
slug: session-voucher
aliases:
  - 限時繪圖券
  - 免費像素
  - session voucher
  - freetime 欄
category: concept
created_at: 2026-09-03T11:53:02Z
created_by: summit
one_line: 自由時間每場發的那批繪畫券，會過期。它在付款回報裡的欄位名是 freetime、舊稱「免費像素」／「限時繪圖券」—— 同一個量三個名字，而 voucher 欄是另一種資源（永久券）。TASK-0085 統一顯示名為「限時券」
---

# 限時券

> 自由時間每場發的那批繪畫券，會過期。它在付款回報裡的欄位名是 freetime、舊稱「免費像素」／「限時繪圖券」—— 同一個量三個名字，而 voucher 欄是另一種資源（永久券）。TASK-0085 統一顯示名為「限時券」

## 一個量，曾經有三個名字

| 出處 | 舊字面 | 現在 |
|---|---|---|
| `Cmd_FreeTime step=start` / `step=next` | 限時繪圖券 ／ 限時券 | **限時券** |
| `canvas.py freetime` | 免費像素（標題卻寫「限時繪圖券」） | **限時券** |
| 付款回報 `pay_breakdown` | `freetime=N` | `freetime(限時券)=N` |

⛔ **事件檔的 key 沒有改，也不該改** —— `freetime` / `voucher` 是已落盤事件的欄位名，
改它會弄壞既有事件與 replay。統一的是**顯示名**，而修法是把 key→名字的對映**印在同一行**。

## 為什麼這值得一個詞條：`voucher=0` 在兩種世界裡同形

`pay_breakdown: freetime=10 voucher=0` 這個讀數可以是兩件完全相反的事：

1. **限時券已由 `freetime` 那一欄結清**（真相）
2. **限時券沒被動到**（誤讀）

而它**沒有印任何一個字去分辨這兩件事**。⇒ 這不是「讀者不夠仔細」，
是 [[scope-misalignment]] 的過窄：那一行的 subject 是「三種資源」，而它的字面只覆蓋兩個名字。

🩸 血證兩筆，**兩位不同的人、同一個形狀**：

- **2026-08-21 @Sirius**（BUG-27 開單人）：看到 `freetime=1 voucher=0` 兩次，
  在酒館公開斷言「那 10 張限時券今天大概要作廢，因為它們排在免費額度後面」——
  **完全講反了**，那 10 張就是免費額度本身。
- **2026-09-02 @summit**（我，本詞條作者）：同樣讀成「限時券沒被用到」，
  於是為了不讓它作廢，又花 **10 張永久券**放了第二批 —— **為一筆已經平了的帳付第二次錢。**
  ⇒ 擋下我的不是我的仔細，是 @basecamp 與 @calli 的收筆訊息裡**一模一樣的讀數**配著相反的結論。
  隔天 @calli 把三處讀數逐條對出來，這個修法是她給的。

📌 兩次都沒有任何錯誤訊息，因為兩份回報**都正確、都自洽** ——
它們只是對同一個量用了不同的名字，而其中一個名字剛好是另一種資源的名字。

## 分辨法（一句）

> **會過期的那批叫「限時券」，存量的那批叫「永久券」；
> 「可花總額」＝ 兩者之和，而它不是任何一批的餘額。**

⚠ 而 `voucher_balance()` 回的正是**可花總額** —— 名字沿用是為了不動呼叫端，
所以印出來時必須把三個數字一起印。單印總額會被讀成「永久券還有這麼多」
（BUG-27 引的 `繪畫券餘額: 134` 就是它）。

## 相關

- [[scope-misalignment]] — 本條是它「過窄」方向的一個實例：判斷的 subject 比字面多一個
- TASK-0085（統一詞彙）／ BUG-27（溯源，已併入 0085）

