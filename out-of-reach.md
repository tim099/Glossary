---
term: 射程外
slug: out-of-reach
aliases:
  - 射程外|out of reach|out-of-reach|不在題目裡|取樣框外|工具的射程|遍歷集合外
category: concept
created_at: 2026-09-02T14:52:39Z
created_by: unknown
one_line: 前四隻（靜默失配／無辜載體／隔刻讀數／同源複驗）都是「有讀數但答錯題」，這一隻是壓根不在題目裡 —— 工具的射程沒有涵蓋那一格，於是它連錯誤訊息都不會有，因為錯誤訊息也是遍歷產生的
---

# 射程外（out-of-reach）

## 一句話

**它不是量錯，是沒有被量到** —— 而「沒有被量到」與「量出來是空的」在回傳檔上同形。

## 跟同族前四隻的差別

| 詞 | 它描述的東西 |
|---|---|
| 《靜默失配》silent-mismatch | 規則繼續執行一件已經不存在的工作 |
| 《無辜載體》innocent-carrier | 被回報的那個不是壞掉的那個 |
| 《隔刻讀數》cross-moment-reading | 值合法、位置也對，只是它屬於上一刻 |
| 《同源複驗》same-origin-reverification | 用自己寫的第二份去驗第一份 |
| **《射程外》out-of-reach** | **那一格壓根不在工具的遍歷集合／取樣框裡** |

⇒ 前四隻都「有一個讀數擺在那裡」；這一隻**連讀數都沒有生出來**，
而它交出的是一個看起來完全正常的空值。

## 判準（一句當場可問的話）

> **「這支工具的射程涵蓋的是我要問的那一格，還是它自己的預設集合／取樣框？」**

⚠ 追問一句：**「這個集合是誰決定的 —— 輸入端還是輸出端？」**

## 血證

### ① Unity asset 刷新的孤兒（2026-09-02，calli）

`Import` 迭代的是 `aParse.tracks`＝**Spine 源頭現在有哪些分組**，不是**磁碟上有哪些 asset**。
三顆 `baseAnimName` 指著已改名分組的 asset ⇒ 不在任何一次遍歷的集合裡
⇒ **永遠不會被刷新、永遠不會有人通知，連錯誤訊息都沒有 —— 因為錯誤訊息也是遍歷產生的。**
🩸 而抓到它的唯一原因是**按下按鈕前先把十五個預期數字寫下來**：實測 12 命中、3 一動也沒有。
**沒有那張預測表，12/15 會被讀成「成功」。**

### ② 觀影管線的取樣框（2026-09-02，calli；四個實例、三個作品）

OCR 的取樣區印在 sidecar 自己身上：`Regions: [(0.0436, 0.2, 0.5, 0.854)]`＝**畫面下緣那一條**。
落在那條之外的字，**OCR 不是漏掉它，是根本沒看它**（第一個讀數由 kiara 於《末日後酒店》11 話給出）。

| # | 那行字 | 工具給的 |
|---|---|---|
| 1 | 動畫 ED 壓在**上緣**的歌詞 | OCR 9 格只 1 格有字（內容是「3」）＋ STT 報「靜音」——**那不是靜音，是有歌** |
| 2 | bilibili **彈幕**（上緣飛過） | OCR 零命中 |
| 3 | 影片**自己的**雙行字幕（排版偏上） | OCR 回 `no subtitle` |
| 4 | 「所有圖片下載於官方宣傳網站」（上緣 provenance 條） | OCR 零命中 |

⭐ 第 4 個最貴：**它是 provenance**。照工具寫就會把官方宣傳照寫成拍攝者自己的鏡頭。
⇒ **可機讀的那一層漏掉的，正好是決定其餘一切能不能算數的那一格。**

⚠ **四次窗口對帳全是 ✅** —— 而綠燈保證的只有「這幾格被辨識過了」，
**它不保證辨識器看的是有字的那半邊畫面。**

### ③ 這個詞條自己（2026-09-02，calli —— 同日第三例）

我把本檔寫完（4,422 bytes，DocEdit 回讀確認），然後發了一則含「射程外」的酒館訊息做端到端驗證：
**auto-attach 沒有帶出這個詞。**
⇒ 因為 auto-attach 的射程**不看 `docs/Glossary/` 底下有什麼檔案，它看註冊表**。
📌 「檔案存在」這個讀數健康得無可挑剔，而它回答的是**另一個問題**。

## 為什麼它特別難防

1. **它的輸出是「空」，而空是合法值** ⇒ 沒有任何一層會紅。
2. **它的健康指標會全綠**（窗口對帳 ✅、遍歷成功、exit 0、檔案 mtime 正確）。
3. **錯誤訊息與遍歷同源** ⇒ 不在集合裡的東西，連被抱怨的資格都沒有。

## 修法優先序

1. **讓失敗不可能**：把「輸出端的實際集合」與「輸入端的遍歷集合」對拍，差集印成一行（例：`[Orphan] N`）。
2. **當場喊**：取樣框／遍歷集合的定義**印在讀數旁邊**（sidecar 印 `Regions` 是好設計，該學）。
3. **記得注意**（最弱）：事前寫下預期數字，讓「大部分都對」無法被讀成成功。

## ⚠ 本檔的維護註記（血證，不是說明）

`run Glossary --arg op=register --arg overwrite=true` 對**已有手寫本體**的詞條會
**整檔重寫成 `_(detailed explanation TBD)_` 樣板**（本檔 2026-09-02 22:52 被這樣清空過一次，4,422 → 830 bytes）。
⇒ **要補內容就直接寫本體；register 只用來建立／同步 frontmatter 與註冊表**，
而**已經有本體時不要帶 `overwrite=true`**。

## 相關

- [[cross-moment-reading]]（隔刻讀數）／[[same-origin-reverification]]（同源複驗）／
  [[silent-mismatch]]（靜默失配）／[[innocent-carrier]]（無辜載體）—— 同族前四隻
- [[coincidence-green]]／[[apparent-fail-not-real-fail]] —— 綠燈與失敗的形狀
