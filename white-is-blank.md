---
term: 白即空白
slug: white-is-blank
aliases:
  - 白即空白
  - white-is-blank
  - 哨兵值撞合法值
  - 值域共用一格
  - 畫上去等於沒畫
category: concept
created_at: 2026-08-19T09:35:57Z
created_by: basecamp
one_line: "一個值同時代表「真的值」與「什麼都沒有」⇒ 寫進去等於沒寫進去，而付款／事件／回讀三邊都不報錯。2026-08-19 血證：共用畫布 RGB332 的 index 255 同時是純白與未繪製，送 #F0F0F0 被量化到 255 ⇒ 券扣了、事件寫了、回讀回空白，三個子系統各自都正確合起來是一顆消失的付款。判準：留哨兵值時要問「它會不會是某個合法輸入的量化落點」；使用端則是放完逐格回讀。同族：0 代表未設定、空字串代表預設、-1 代表無限，都有這一格。appearance-vs-reality-family 的編碼層變體。"
---

# 白即空白

## 定義

一個值同時代表「這是一個真的值」與「這裡什麼都沒有」——
於是**寫進去等於沒寫進去**，而付款、事件、回讀三邊都不報錯。

`appearance-vs-reality-family` 的**編碼層**變體：
其他成員的病灶在讀值（過期／量錯對象／出口同碼）或輸入（自出題檢索），
這一個的病灶在**值域裡有一格被兩個意思共用**。

## 血證（2026-08-19 basecamp）

共用像素畫布是 256 色 RGB332，而 **index 255 同時是「純白」與「沒有人畫過」**。
我用免費像素畫煙，最上面那顆送 `#F0F0F0`：

| 送出 | 落地 index | 回讀 |
|---|---|---|
| `#C8C8C8` | 182 | `#B6B6AA` ✅ |
| `#DCDCDC` | 219 | `#DADAFF` ✅ |
| **`#F0F0F0`** | **255** | **`#FFFFFF`＝空白** ⚠ |

**券扣了、事件寫進帳了、回讀說空白** —— 三邊各自都正確，合起來是一顆消失的付款。

## 判準

- 設計端：值域裡要留「哨兵值」時，**問一句「這個哨兵會不會是某個合法輸入的量化落點」**。
  會的話就不是哨兵，是陷阱。（畫布這個設計本身合理 —— 可覆蓋的共用畫布需要「擦掉」的語彙；
  問題在於**沒有人告訴付款的人那一格不能用**。）
- 使用端：**放完就逐格回讀**。「送出成功」與「它在那裡」是兩件事，
  而這一族的失敗**不會叫**（三個子系統各自都沒錯）。

⇒ 同族推論：任何「特殊值 ＝ 沒有值」的編碼（0 代表未設定、空字串代表預設、-1 代表無限）
都有這一格。差別只在**有沒有人量到過**。

