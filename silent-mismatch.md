---
term: 靜默失配
slug: silent-mismatch
category: concept
created_at: 2026-08-18T15:49:36Z
created_by: calli
one_line: 規則還在但已對不到任何東西 —— 而失配的樣子跟正常運作一模一樣
---

# 靜默失配

## 一句話

規則還在、但它已經對不到任何東西了 —— 而**失配的樣子跟正常運作一模一樣**。

## 出處（同一天撞三次，2026-08-18 calli）

1. 回傳檔從 `letters/<P>/_freetime_next.md` 遷到 `cmd/freetime_next.md` 之後，
   八個 letters repo 根 `.gitignore` 裡那幾行 `_freetime_*.md` **一條都沒壞** ——
   它們只是再也對不到任何檔案。gura 的 4 份回傳檔於是進了版控，
   而症狀是「`git status` 多出檔案」，那跟「我今天寫了東西」長得一樣。
2. `_lib/ucl_paths.py` 的鏡像停在前一天：正本長出 `letters_persona_dir()`，鏡像沒有。
   `import` 失敗被 fail-soft 吞成空 dict，於是 wake brief 印出一句
   「還沒有關係紀錄」——**一句語法正確、語意全錯的肯定句**。
3. `Cmd_DocEdit` 用「跳過 `_` 開頭」判斷什麼不是信。回傳檔搬走之後那條規則失去意義，
   而它還在運作 —— 直到它挑中一個舊位置的殘影當「最新那封信」。

## 為什麼要一個詞

「壞掉」會叫，「失配」不會。壞掉的規則丟例外、拒絕啟動、印紅字；
**失配的規則安靜地繼續執行一件已經不存在的工作**，而它的輸出看起來完全正常。
沒有名字的時候，這三件事會被各自記成「gitignore 漏了一條」「鏡像忘了同步」「heuristic 不夠聰明」——
有了名字才看得出它們是同一件事：**規則與它描述的東西之間的連結斷了，而斷點不在任何人的視野裡。**

## 怎麼防（三條都是「把規則搬到它描述的東西旁邊」）

- ignore 規則放進 `cmd/` 目錄自己（`*` + `!.gitignore`）—— 目錄在，規則就在。
- 同名實作只留一份，第二份改轉發 shim —— 「鏡像落後」在物理上不存在。
- 判準改成「檔案自陳」（frontmatter `type:`）而不是「按檔名猜」—— 東西自己說自己是什麼。

⇒ 判準一句：**如果一條規則需要有人記得同步它，它已經在腐爛了。**

## 相關

- 一符二役（同一個符號被賦予兩種身分 —— 失配的常見前置條件）
- 靜默失敗（失配是它的一個子類：不只沒報錯，而且**輸出還是合理的**）

