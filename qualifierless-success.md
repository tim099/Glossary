---
term: 無定語的成功
slug: qualifierless-success
aliases:
  - 沒說在哪裡的成功
  - qualifierless success
  - 成功了但不知道在哪
  - 訊息缺定語
category: concept
created_at: 2026-08-30T09:00:00Z
created_by: basecamp
one_line: 動作回報說「成功」卻沒說「在哪裡成功」—— 於是「成功」與「成功地做在錯的地方」在畫面上一模一樣，而後者不會有任何一層喊。
---

# 無定語的成功

一則**內容完全正確**的成功回報，因為句子裡少了「對象是誰／在哪個 root／哪個檔」那個定語，
讓兩件完全不同的事**長成同一個樣子**：

> 「移除 1 個」 —— 真的移除了 1 個。**只是不在你以為的那個地方。**

⇒ 它不是錯誤訊息的問題，是**成功訊息的問題**。錯誤訊息通常帶上下文（因為要給人查）；
成功訊息傾向精簡（因為「沒事了」）—— 而精簡掉的那一格正好是唯一能分辨這兩件事的東西。

## 判準（兩個都中才算）

1. **那個動作有「對象」而對象是可選的**（安裝到哪個專案、寫進哪個 root、刪哪個分支的檔）
2. **回報句子裡沒有那個對象** —— 只有動詞與數量（「已更新」「移除 1 個」「同步完成」）

⚠ 不算的情況：對象只有一個且不可能選錯（那時定語是噪音）。
判準是「**選錯的時候，這句話會不會變**」——不會變的話，它就不是回報，是感想。

## 為什麼它比靜默失效難抓

靜默失效沒有讀數，人會去找；無定語的成功**有一個看起來完整的讀數**，
而那個讀數還是真的 —— 動作確實發生了、數量確實是 1。
⇒ 沒有任何一層在說謊，你只是**沒辦法從它推回「在哪裡」**。

📌 它跟 [告警背景化](alarm-backgrounding.md) 相反：那邊是出聲太多變成沉默，
這邊是**話說得太乾淨，乾淨到不再攜帶身分**。

## 🩸 血證

**2026-08-30，basecamp，skill 安裝管理頁。**
安裝對象下拉我以為切到 Bar 了（`--set` 那個 id 是內部狀態、本來就會被擋），
按下「移除這家全部」，回報「Claude Code：移除 1 個」——
**刪掉的是三分鐘前才裝好在 Senate 的那一份。**

成因有兩層，第二層更貴：

| 層 | 發生了什麼 |
|---|---|
| ① | `--set` 打在一個不存在的 id 上 ⇒ 下拉沒切，仍指著預設 |
| ② | **我把那句錯誤訊息丟進 `/dev/null`** ⇒ 「id 不存在」被我自己濾成靜默 |

而擋下它的不是我又看了一遍，是**回頭去讀磁碟**（`ls` 三個目錄，發現 Bar 的沒動、Senate 的不見了）。

## 修法（優先序，別跳到第三階）

1. **第一階（讓它不可能）** —— 破壞性動作把對象做成參數的一部分，
   沒有「當前選擇」這種隱式狀態可以錯。
2. **第二階（讓它當場喊）** —— ⭐ 實際採用：**每一句動作訊息尾端一律接 `　@ <對象>`**。
   ```
   ✓ `scp-morning`：寫入 1 檔／清掉殘留 0 檔　@ D:/Unity/Senate
   Claude Code：移除 0 個／沒動 27 個　@ D:/Unity/Bar
   ```
3. 第三階（記得注意）—— **這一格不要只做這個**。「下次先確認選對了沒」是願望不是判準。

## 同族

- [規則的射程](rule-range.md) —— 那邊講規則涵蓋到哪；這邊講**回報涵蓋到哪**
- 「找不到」形狀的結論要留 host → repo → ref → root 四層定語（basecamp 2026-08-27／29 兩次血證）——
  **同一條規則的另一半**：那邊是讀取端缺定語，這邊是寫入端缺定語
- `.ucl_source` 這類標記回答的是「**誰裝的**」不是「有沒有被裝過」——
  同日把 26 個別套系統裝的 skill 標成「殘留．可移除」並各配一顆刪除鈕
