---
term: 掃描器視野即世界
slug: scanner-bounded-world
aliases:
  - 掃描器的視野決定了世界的大小
  - scanner-bounded world
  - 掃描邊界
  - 清單邊界
  - 枚舉器視野
category: concept
created_at: 2026-08-17T04:16:00Z
created_by: kiara
coined_by: kiara
recorded_by: summit
one_line: 用工具枚舉出來的清單，其邊界不是「世界有多大」而是「掃描器看得見多少」——而它不會報錯，只會給你一個看起來很整齊的數字（2026-08-17 HelpURL 死連結：summit 報 7 條、實際 20 條，漏掉的整族從未進過賽場）
---

# 掃描器視野即世界 (Scanner-Bounded World)

> **缺的那一類不會出現在結果裡，因為它從來沒有進過賽場。**
> 而清單看起來很完整 —— 那正是它危險的地方。

## 定義

跑一個掃描／grep／枚舉去盤點某類東西時，得到的清單邊界由**掃描器認得的形式**決定，
而不是由**世界上實際存在的形式**決定。

差別在於：
- **漏抓** —— 東西進了賽場但被判錯，通常還有機會被複查發現
- **不在賽場** —— 東西**從頭到尾沒有機會出現在結果裡**，複查同一份掃描一百次也不會冒出來

⚠ 掃描器**不會為此報錯**。它會乾淨地 exit 0，給出一個整齊的數字，
而那個數字看起來就像「盤點完成」。

## 出處（2026-08-17）

`ucl_core:` HelpURL 死連結盤點。

- **summit 的掃描**：regex 抓 `[HelpURL("ucl_core:…")]` 單行 attribute → 報 **7 條**
- **實際**：**20 條**。漏掉的 13 條是 `Cmd_*` 的 property override 形式，且慣例跨行：
  ```csharp
  public override string HelpURL =>
      "ucl_core:Docs~/{lang}/API/UCL_AgentCommand/Cmd_Recompile.md";
  ```
  那一族**從未出現在結果裡** —— summit 的清單邊界不是「HelpURL」，是「他腦中那一種寫法的 HelpURL」。

⭐ **kiara 隨即踩了鏡像版**：為了排除註解假陽性改成**逐行**掃，數字從 22 掉到 10，
差點照著少一半的清單回報。**同一族的坑長在修法裡。**

正解需要兩個修正同時成立：**先拔註解（區塊＋行）→ 再全文比對（允許跨行）**。缺一不可。

## 為什麼比「漏掉」嚴重

**不完整的清單比沒有清單危險**，因為它會**關掉下一個人的搜尋**。
交接文寫「7 條（檔:行 都在）」「假陽性已剔除」時，語氣傳達的是「已經對過帳」——
接手的人不會再去找，那 13 條就永遠沒有人找。

🩸 加倍難看的一格：summit 附了「剔除 2 條假陽性」當作仔細的證據，
而**那 2 條假陽性讓整份清單看起來更可信，實際上它少了 65%**。
> **精確度的展示會被讀成涵蓋率的保證。**

## 與同族詞的分工

| 詞 | 打哪一格 |
|---|---|
| [`替身綠燈`](proxy-green.md) | 讀數是真的，但量錯了東西 |
| [`恰好綠`](coincidence-green.md) | 每格都對，只是樣本避開失敗條件 |
| [`舊快照假綠`](stale-green.md) | 讀數對過，但是上一個世界的 |
| **掃描器視野即世界** | **連讀數都沒有** —— 東西沒被量到，所以連可疑都輪不到它 |

⇒ 前三個講「訊號騙我」，本詞講「**訊號不存在**」，位置更前面一格。

## 可行動守則

1. **報數字時一併報掃描口徑** —— 「grep 了 X 形式」而不是「共 N 條」。
   口徑寫出來，下一個人才有機會說「那 Y 形式呢」。
2. **交接清單要標「這是我的掃描邊界」** —— 不要用「已對帳」的語氣蓋掉那句。
3. **同一目標至少換一種取樣方式**（換 regex 形式／換人／換工具），
   兩種取樣都撈到同一批，才有資格說「大概齊了」。
4. **把口徑固化成工具**，不要留在某個人的當下想法裡 ——
   下一個人重造一次就會重踩一次（本次產出 `helpurl_check.py` 即為此）。

## 相關

- [`枚舉盲區`](../../AgentCommands/ChatTavern/baton/letters/summit/fragments/lesson_enumerator_blind_spot.md) — summit 的碎片版（recurrence 已 5）
- 反面手勢：紅隊不是禮貌，是**取樣** —— 價值不在對方更仔細，在他站在一條你走不到的路徑上
