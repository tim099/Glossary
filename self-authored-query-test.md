---
term: 自出題檢索
slug: self-authored-query-test
aliases:
  - 自出題檢索
  - self-authored-query-test
  - 自己出題自己改
  - 鉤子自測
  - 用自己寫的問法測檢索
category: concept
created_at: 2026-08-19T04:05:44Z
created_by: basecamp
one_line: 驗語意檢索時拿自己寫進索引的那組問法當測試題 —— 量到的不是「撈得到」而是「我跟我自己用詞一致」。2026-08-19 血證：KB target coding 用自寫鉤子測三題全第一（0.77/0.76/0.72），換沒寫過的問法三題掛兩題（merge conflict 那題撈到 code-review、premature completion 那題完全沒撈到）。判準：驗檢索要用不是自己寫的問法，第一輪全綠只證明鉤子跟作者一致，而作者本來就知道東西在哪。appearance-vs-reality-family 的輸入端變體。
---

# 自出題檢索

## 定義

驗語意檢索時，拿**自己寫進索引的那組問法**當測試題 —— 於是分數量到的不是「這條知識撈得到」，
而是「我跟我自己用詞一致」。**自己出題、自己改考卷。**

`appearance-vs-reality-family` 的**輸入端**變體：其他成員的病灶在讀值（過期／量錯對象／出口同碼），
這一個的病灶在**題目本身是被告寫的**。

## 血證（2026-08-19 basecamp）

新建 KB target `coding`（8 檔 90 chunks），每檔開頭寫了一段「## 會這樣問」當檢索鉤子。

- **第一輪**用鉤子裡那三句測 → 全部第一名（0.7715 / 0.7627 / 0.722）。看起來完全可以收工。
- **第二輪**換成沒寫過的問法 → 三題掛兩題：
  「同事跟我改到同一個檔案，git 不給我合併」撈到 `code-review`（0.5984），
  正確那篇只排第二（0.5982）；「怎麼讓 AI 不要草草宣布任務完成」**完全沒撈到**談 premature completion 的那篇。

修法：鉤子加寬到含真實症狀字串（`CONFLICT (content)`、`<<<<<<< HEAD`）與英文術語 → 重測兩題都回第一名。

## 判準

**驗檢索一定要用不是自己寫的那組問法。** 第一輪全綠只證明「鉤子跟作者一致」，
而作者恰恰是最不需要這個索引的人 —— 他本來就知道東西在哪。

⇒ 同族推論：**撈不到跟「這條知識不存在」在畫面上長得一模一樣**，所以檢索的失敗天生不會叫。
攔截來源仍然只有兩個 —— 別人的問法，或一組不是我挑的輸入。「我再想幾句問法」不在名單上。

