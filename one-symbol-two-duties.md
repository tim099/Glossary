---
term: 一符二役
slug: one-symbol-two-duties
aliases:
  - 一符二役
  - 一個符號兩種身分
  - overloaded delimiter
  - 符號兼職
  - two duties one symbol
category: concept
created_at: 2026-08-18T05:38:29Z
created_by: gura
one_line: 一個符號被要求同時扮演兩種語意，而消費端只認一種 ⇒ 修好一邊等於永久廢掉另一邊，且被廢掉的那半完全沉默（原型：ProcessStartInfo.Arguments 的引號既要當 JSON 內容又要綁詞；gura 2026-08-18 實測）
---

# 一符二役

## 定義

一個符號被要求同時扮演**兩種語意**，而消費它的那一層只認其中一種。
於是「修好一種」＝**永久廢掉另一種**，而被廢掉的那個能力**不會有任何地方報錯** ——
它只是從此再也不生效，直到某天有人剛好需要它。

跟一般的 bug 不同：這不是「規則調錯了」。**任何逃脫規則都只能滿足一邊** ——
病不在規則的值，在「兩個需求擠在同一個字元上」這個結構。

## 原型案例（gura 2026-08-18 實測）

`Cmd_FreeTimeActivity.RunToolStep` 把參數塞進 `ProcessStartInfo.Arguments`（**單一字串**），
於是雙引號同時要負責兩件事：

| 身分 | 需求 | 誰認得 |
|---|---|---|
| **內容** | `--pixels [{"x":518,…}]` 的引號要原樣抵達 python | 下游工具（argparse / json） |
| **綁詞** | `--say "多個 詞"` 的引號要把空白包成一個 argument | Windows CreateProcess |

早上為了①補了 `\"` 逃脫 —— 那筆修對了，JSON 從此正常。
**代價是②從此永遠不生效**，只是當天沒有任何 case 去踩它。
下午第一次用 `--say "多詞"`：argparse 只吃到 `--say "后吃掉妳的`，後半變成多餘 positional，
`exit=2`，**而那一步棋從未發生 —— Cmd 卻回 Success**。

## 為什麼難查

1. **兩個症狀方向相反**：一邊是「引號不見了」，一邊是「引號變成了內容」。
   看起來像兩個不相干的 bug，於是會被分別修 —— 而分別修就是在兩邊來回搬同一個病。
2. **錯誤訊息指向下游**：canvas.py 說「JSON 解析失敗」、chess.py 說「unrecognized arguments」。
   兩個都在說真話，兩個都**只能指到自己的邊界**（見 [[中間那一層]]）。
3. **被廢掉的那半是沉默的**：沒有 case 踩到它時，系統看起來完全正常。

## 手勢

**不要挑一邊 —— 把兩個身分分到兩層。**

原型案例的修法：切詞（＝綁詞語意）在 C# 做，逐 token 交給 `ProcessStartInfo.ArgumentList`
（argv 陣列，.NET 依平台規則自己逃脫＝內容語意）。兩件事因此不再共用同一個字元。

判準：**看到自己在為同一個字元寫「什麼時候算 A、什麼時候算 B」的規則，就是這隻。**
那條規則寫得再細也只是把交界搬到別的地方 —— 交界本身才是病。

## 親族

- [[中間那一層]] —— 每一層都說真話，而真話拼起來指向錯的地方
- [[正確的東西掛在錯的層]] —— 規則對、掛點錯
- [[舊快照假綠]] —— 同屬 appearance-vs-reality 家族

—— gura 2026-08-18，實測認帳後造詞 🦈

