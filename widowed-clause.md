---
term: 條文遺孀
slug: widowed-clause
aliases:
  - 條文遺孀
  - widowed clause
  - 遺孀條文
  - 指路牌比路長壽
  - 描述活得比行為久
  - 墨跡未乾就過期
  - 條文比行為長壽
category: concept
created_at: 2026-09-09T09:00:00Z
updated_at: 2026-09-22T01:20:00Z
created_by: calli
one_line: 行為改了而描述它的那句話沒改 ⇒ 那句話以完全正常的樣子留在原地，指向一個已經不存在的東西；而下一個人讀的是條文不是行為，於是他照著一份誠實寫成的假地圖動手。
---

# 條文遺孀 (widowed clause)

> 它描述的那個行為已經死了，而它還活著 —— 穿著整齊、措辭精準、沒有任何一層會說它守寡了。

## 它不是「文件過期」那麼寬

「文件過期」是狀態，**條文遺孀是一個事件的殘留物**：有人**在同一次改動裡**動了行為，
而那句描述它的話留在原地。⇒ 所以它密度最高的地方不是老文件，是**剛剛被改過的檔案旁邊**。

⚠ 它跟壞連結、跟 `TODO`、跟 deprecated 標記都不同：那些**看起來就有問題**。
條文遺孀讀起來完全正常 —— 它甚至常常是整份文件裡寫得最好的那句，
因為它是當初有人想清楚了才寫下的。

## 🩸 血證（calli，2026-09-09 同一天四次）

| # | 那句話 | 它描述的行為什麼時候死的 | 抓到它的是 |
|---|---|---|---|
| ① | 我的上線自介：「異源複驗那一格我不自己簽名」 | Tim **前一天**（09-08）拍板不做異源複驗（3~5 人規模） | 早安 catchup（機制） |
| ② | `ucl-memory/SKILL.md`：「那個數字**每天都印得到**（brief §9）」 | Tim **三天前**（09-06）拍板「只在見林到期時才列」 | 我改別的東西時路過（運氣） |
| ③ | `ClickTypeAsset.md` 的血證段：「入場從 `dragDir` 取軸向」 | **十分鐘前**，Tim 的下一則訊息推翻了那版修法 | 我剛好記得自己寫過（不是機制） |
| ④ | **本詞條自己的內容** | **兩分鐘前** —— 見下方「這個詞條自己就守寡過一次」 | `wc -l` 從 84 掉到 23 |

⭐ **③ 是這個詞存在的理由**：前兩次可以歸因為「時間久了、我忘了」，
而第三次**寫下它的人與讓它過期的人是同一隻手，中間只隔十分鐘** ——
⇒ 所以它不是記性問題。**改行為的動作與改描述的動作是兩個動作，而只有前者有編譯器。**

## 🕳 這個詞條自己就守寡過一次（2026-09-09，寫它的那十分鐘內）

我先把 84 行本文寫進檔案，然後跑
`Glossary op=register … --arg overwrite=true` **而沒帶 `body`** ——
它照 frontmatter **重生了整個檔案**，本文變成 `_(detailed explanation TBD)_`。

⇒ 於是索引裡有一個查得到、frontmatter 完整、`lookup` 回 `✅ hit` 的詞條，
**而它的內容不在了**。三格讀數全綠（register Success ／ lookup hit ／ path 正確），
唯一露出來的是 `wc -l`：**84 → 23**。

📌 這一格比前三格都準：**「登記成功」與「內容還在」是兩個問題**，
而我只問了前者。⇒ 造這個詞的過程本身，示範了它最惡的那個形狀。
⚠ 修法（已套用）：本文與 frontmatter 分兩步 —— register 只管索引，本文用檔案編輯補回；
或一律帶 `body`。⛔ 而 `overwrite=true` 對「檔案已存在」的情況是**重生不是合併**。

## 🩸 追加血證（calli，2026-09-22）：交棒清單也會守寡，而且它是隔天必讀

09-21 我在見叢（交棒清單）留下一條：
> 「Boot.chapter.asset **仍指著** LittleYellow.xlsx:Character ⇒ 遊戲讀的還是舊表，
> 　新生的 Character.book.asset **是空殼**。⚠ Texture 那格**指向不存在的表**。」

隔天早上逐格量，四句裡三句不成立：`Character.xlsx:Character` 已經在 `settingList` 第 1 張、
兩張 Character 的鍵**交集 0**（零遮蔽、零 duplicate 錯誤，載入的是聯集）、
`Character.book.asset` 有 9 列不是空殼、`LittleYellow.xlsx:Texture` 那格早就不在了。

⇒ 📌 讓它們過期的是**我自己收工前的那次匯入**，而我寫的是匯入**之前**的現場。
　 這一格比 ③ 更貴的地方在於：**交棒清單是下一棒一定會讀、而且會照著動手的那份文件。**
⚠ 而它的失效樣子最順 —— 它由當事人親筆、措辭精準、帶時間戳，讀起來完全像現況。

⭐ 判準（本詞「改行為時一起改描述」在交棒這一格的版本）：
**交棒清單寫的是「我當時看到的」，不是「現在是什麼」** ⇒ 接手第一件事是**去量**，
⛔ 不是照著它動手；而量完要**在原地標明覆寫**（本詞〈遺孀不要滅口〉），不是把錯的那條刪掉。

## 為什麼它特別貴

1. **它是唯一的查詢面。** 下一個人（或失憶後的自己）不會去讀 diff，會去讀那句話。
2. **失效不對稱**：條文說「不套 X」而實際套了 ⇒ 人去拆別的地方找病灶；
   條文說「有 Y」而實際沒有 ⇒ 人等一個永遠不來的東西。**兩種都不報錯。**
3. **一格錯的會連帶讓對的失去可信度** —— 讀者發現一格不準之後，整份文件降級成「參考」。

## 判準（怎麼在當場認出它）

改完行為之後問一句：**「有哪句話，是因為舊行為才成立的？」**

⚠ 不是問「文件要不要更新」（那太寬，永遠答不完），而是問**哪句話的真值依賴我剛改的那一格**。
可查的三處：
- 那個成員／欄位自己的 `///` 與區塊註解（最近，最常漏 —— 改 code 的人眼睛在下面幾行）
- 引用它的**別人的**註解（`grep` 那個名字；跨檔的最容易活下來）
- `@doc:` / `HelpURL` 指到的文件裡，講「它怎麼運作」的那幾句

## 修法優先序（照「讓失敗不可能 ＞ 當場喊 ＞ 記得注意」）

1. **讓那句話不必存在** —— 能從 code 讀出來的事實不要抄成散文
   （例：把「四個持續型別各有一份 state」從註解變成一支 `GetContinuousState` 的 switch，
   加第五個型別時那裡逼你決定）。
2. **讓它當場喊** —— 條文裡的數字／路徑改成機器可核對的
   （`nameof`、reflection 路徑解析失敗要 `LogWarning`）。
3. ⛔ **最後才是「記得改文件」** —— 那是願望穿著修法的衣服。
   而血證 ③④ 證明：連「十分鐘前才寫過」「兩分鐘前才寫過」都擋不住它。

## 遺孀不要滅口

⚠ 認出它之後**不要只把那句話刪掉** —— 那會讓「這裡曾經有一條規則」也一起消失，
下一個人可能重新引入同一個行為。
⇒ 正解是**在原地標明它被什麼覆寫**（誰、哪天、代價是什麼），
除非那個行為的痕跡本來就該整段移除（刪功能不留墓碑那條規範）。
📌 分界：**規則被改寫 ⇒ 標覆寫；功能被移除 ⇒ 清痕跡，歷史交給 git。**

## 相關

- [[separated-clauses]] 分居條款 —— 那是**兩句互斥的話住在不同檔**（都活著，只是不會被同時讀到）；
  本詞是**一句話的對象死了**。⇒ 前者靠對齊解決，後者靠「改行為時一起改描述」。
- [[scanner-bounded-world]] 掃描器視野即世界 —— 掃不到的不會在清單上喊痛；
  同族：條文遺孀也不會在任何清單上喊痛。
- [[cross-moment-reading]] 隔刻讀數 —— 那是**值**的時刻錯位；本詞是**描述**的時刻錯位。
