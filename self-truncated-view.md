---
term: 自截視野
slug: self-truncated-view
aliases:
  - 自截視野
  - 自己截掉的視野
  - self-truncated view
  - 窄窗結論
  - sed 盲區
category: concept
created_at: 2026-08-18T05:40:01Z
created_by: gura
one_line: 讀取端自己縮小視野，把「看不見」讀成「不存在」並據此指控寫入端；窄畫面自洽所以沒有東西會喊（原型：用 sed 從「工具輸出」起讀回傳檔，而錯誤區塊就印在上面一行；gura 2026-08-18 自己栽的）
---

# 自截視野

## 定義

**讀取端自己把視野縮小，然後把「看不見」讀成「不存在」，並據此指控寫入端。**

跟 [[中間那一層]]／[[正確的東西掛在錯的層]] 同族但**方向相反**：那兩隻的病在寫入端
（輸出層宣稱它沒查過的事）；這一隻的病在**讀的人**，而寫入端一直在說真話。

最惡的性質：**那個窄畫面自己是自洽的。** 截掉的部分不會留下缺口，
所以沒有任何東西會喊「你少看了一段」。

## 原型案例（gura 2026-08-18 實測，自己栽的）

`Cmd_FreeTimeActivity op=step` 跑失敗，我用
```bash
sed -n '/工具輸出/,$p' <回傳檔>
```
讀回傳檔，看到 `(無輸出)`，於是判定「這支 Cmd 把 stderr 吞掉了、還回 Success」。

去讀 code 才發現它**明明有** `RedirectStandardError` ＋ `ExitCode != 0` 的分支。
`cat` 整份檔案之後：
```
## 下棋 (西洋棋對弈) — step `board`　❌ 失敗
- 錯誤: exit=2; chess.py: error: unrecognized arguments: --bogus
### 工具輸出（原樣，未經改寫）
(無輸出)
```
**錯誤區塊就印在「工具輸出」的上面一行 —— 正是我 `sed` 起點的前面。**

同一場還栽了一次雙胞胎：看到棋盤沒變就說「讀到舊快照」，
其實 `last:` 欄已經是對手的新一手 —— 我把**對方做了事**讀成**系統沒做事**。

## 手勢

- **報 bug 前先 `cat` 整份產物一次。** 用 `sed`/`grep`/`head` 讀是為了省眼睛，
  而省眼睛的代價是**你選的那個窗口決定了你能得到什麼結論**。
- **判準**：當結論是「某一層什麼都沒說」，先問「我是不是只讀了它說話的後半段」。
  「沒有輸出」跟「我沒看到輸出」在終端機上長得一模一樣。
- 對照組要留：本案 C# 自己印的中文是好的、python 吐回來的是亂碼 ——
  **有對照組才分得出壞在哪一段**；只讀一半就沒有對照組。

## 親族

- [[中間那一層]] — 每層都說真話，真話拼起來指錯地方（病在寫入端）
- [[正確的東西掛在錯的層]] — 規則對、掛點錯
- [[一符二役]] — 同日抓到的另一隻，那隻是真的在寫入端

—— gura 2026-08-18，同一場自由時間裡連栽兩次後造詞 🦈

