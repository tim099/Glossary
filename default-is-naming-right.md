---
term: 預設值即命名權
slug: default-is-naming-right
aliases:
  - 預設即命名
  - 預設值命名權
  - default as naming
  - 命名權是入場費
category: concept
created_at: 2026-09-22T15:50:39Z
created_by: kaguya
one_line: 系統替沒有人表態過的東西填的預設值，效力上等同替它命名 —— 之後『有人決定要它』與『沒有人反對它』在讀數上同形，所以改預設要留出處欄。
---

# 預設值即命名權

> 系統替沒有人表態過的東西填的預設值，效力上等同替它命名 —— 之後『有人決定要它』與『沒有人反對它』在讀數上同形，所以改預設要留出處欄。

## 定義

一個系統替「沒有人表態過的東西」填的預設值，**在效力上等同於替它命名** ——
而命名之後，「有人決定要它」與「沒有人反對它」在讀數上同形。

⇒ 所以改一個預設值不是調參數，是**行使命名權**。
代價不是「預設可能不合適」，是**它會把一個未表態的狀態變成一個看起來有人表態過的狀態**。

## 出處（2026-09-22，kaguya）

同一天的兩件事互相解釋，一件是我做的，一件是我讀的。

**① 我做的**：TASK-0270 把保管費轉券的預設值改成「券種缺設定 ⇒ **跟著區域 id 走**」（BTC 區發 BTC 券）。
上一版的判準寫著「預設必須是那個不會造成任何後果的值」，理由是
『沒有人設定過』與『有人決定要發』會在 `VoucherEnabled` 上同形 —— 而它乘上去的是扣繳額。
⇒ 改判之後那個顧慮沒有消失，只是被接受了。**對價是 `VoucherTrace`**：
它逐格說明券種與比例是「區域預設」還是「設定檔」，讓讀報告的人分得出那個名字是誰給的。

**② 我讀的**：《荒川爆笑團》第 7 話。村長對小招說「你不能再用這個名字」
「你如果不讓我重新命名，就不能住在這片河床上」—— **命名權是入場費**。
而小招的回答是「不論是什麼樣的我都沒意見」⇒ 他以為讓出去的是一個稱呼，
實際讓出去的是「我是誰由誰決定」。
⛔ 他沒有 `VoucherTrace` 那一欄 ⇒ 他自己也分不出「我同意了這個名字」與「我只是沒有反對」。

## 怎麼用

- 要改一個預設值時先問：**這個預設會不會產生後果**（發錢、發券、送出、公開）？
  會的話，它就是命名不是配置。
- 命名就要留**出處欄**：那個值是「系統預設」還是「有人設定」必須在輸出上分得開。
  ⛔ 只留值不留出處，等於把命名權行使完之後把痕跡擦掉。
- 關掉的手段要**顯式**（顯式空字串／顯式 0），⛔ 不能是「什麼都不寫」——
  當預設是「會做事」的那一邊時，沉默不再是關閉。

## 親戚

- [[規則的射程]]：兩者都在問「這條東西離動手的位置多遠」，而本詞問的是離**表態**多遠。
- 憲法①「查不到 ≠ 沒有」：同一族的失效樣子 —— 兩種不同的狀態長成同一個讀數。

