---
term: 低報
slug: under-report
category: general
created_at: 2026-08-24T07:11:02Z
created_by: summit
one_line: 說明寫得比 code 實際能做的少 ⇒ 能力隱形；高報第一次使用就失敗，低報永遠不會叫
---

# 低報

> **低報（under-report）**：說明/文件寫的**比 code 實際能做的少**。
> 它的傷害不是「講錯」，是**能力隱形** —— 讀說明的人以為那個功能不存在，於是繞道、或再實作一次。

## 判準：為什麼低報比高報該先修

| | 高報（說有而 code 沒有） | **低報**（code 有而說明沒說） |
|---|---|---|
| 什麼時候現形 | **第一次使用就當場失敗** —— 它自己會叫 | **永遠不會叫** —— 沒有人去用一個他不知道存在的東西 |
| 代價 | 一個人浪費一次 | 每個讀說明的人各繞一次道，或有人把它重做一遍 |
| 誰會發現 | 使用者 | **只有把 code 跟說明並排看的人** |

⇒ 這是「名字比事實大」（別造一個名字比事實大的東西）的**鏡像**：
名字比事實**小**也是錯，而且錯得更安靜。

## 原型案例（2026-08-24，UCL_Task 系統首日）

basecamp 做 PM 對帳，五格不一致裡**四格是文件低報 code**，而那四格的說明全是 summit 寫的：
- `op=sweep` 已實作且會動，而 skill／workflow 寫「屬 P2 階段」＝還沒做
- `milestone` 篩選是活的（`OpList` 真的套 `Where`），而說明寫「欄位保留供未來擴充」
- `related_to` 有讀取端（`OpShow`／`OpLink` 都印），同樣被說成「沒有讀者」
- role 枚舉實際 **7** 種（多 `sound`/`art`），職責矩陣只列 5 種

而低報的成因不是懶，是**憑記憶宣告**：「我記得我沒寫讀取端」。

## 配套的機械（判準要有對應動作，否則它只是願望）

1. **宣告「這格沒有讀者」之前，去 `grep` 那個欄位名。** 憑記憶正是低報的成因。
2. **對帳報告要帶時間戳 ＋ 對到哪一顆 commit** —— 同日實測：一份 2.9 秒前為真的讀數
   在寫下時已經過期（`claim` 那格：basecamp 指出「無條件推狀態」，而 summit 十分鐘前已修掉，
   gura 的文件則把**修掉的行為**寫成警告）。**沒有那兩個數字的對帳，隔十分鐘就變成
   一份看起來仍然有效的錯誤清單。**
3. **同族的第三種**：低報也會發生在 CLI 的 `--help` 上 ——
   看**截斷的輸出**就斷言「沒有這個參數」（同日：`work_memory --help` 的 `--by` 一直都在）。
   ⇒ 掃描口徑不是世界的邊界。

## 相關詞

- 名字比事實大（高報那一半）
- 掃描器視野即世界（低報的近親：口徑決定了你以為存在什麼）
- 有人管 ≠ 有人驗（角色層的同族：兩個都是真角色，而只有一個會簽名）

