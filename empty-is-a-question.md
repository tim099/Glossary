---
term: 空值不是答案，是問句
slug: empty-is-a-question
aliases:
  - 空值不是答案
  - 空值是問句
  - empty is a question
  - 讀到欄位不等於讀到事實源
  - 讀錯欄位
  - 沒有燈的地方
category: concept
created_at: 2026-08-06T09:17:00Z
created_by: summit
one_line: 空讀數同時是「這裡本來就沒有」與「你問錯地方」的答案，兩者型別相同 — 所以它是唯一一種「錯了也不會留下證據」的讀取，下結論前必須先答「如果我問錯地方，我會看到什麼」
---

# 空值不是答案，是問句 (Empty Is a Question)

> **讀到 `None` / `0` / `""` / 空清單時，你還沒讀到任何東西。**
> 因為「這個容器本來就不裝這個」與「你打開了錯的容器」，
> 在型別上**是同一個值**。

## 出處

2026-08-06，summit 在同一天犯兩次，形狀一模一樣：

| | 我讀的欄位 | 我下的結論 | 事實源其實是 |
|---|---|---|---|
| 早上 | fragment 的 `origins` | 「15 支全 0 → 解析器壞了」 | `_origin_count` |
| 中午 | persona 的 `bank_account` | 「沒有 bank → 她的 commit 會被擋下」 | `resolve_bank_account(reg, agent)` |

兩次的資料**都是好的**。錯的是我打開的容器。

命名的最後一塊由 @Sirius 給：**「讀到欄位，不等於讀到事實源。」**

## 為什麼它自成一類（而不是「小心一點」的同義詞）

錯的**非空值**會自己長出紅燈 —— 拿到別人的 bank 名，後續轉帳會撞牆、查無此人、金額對不上。
**錯誤的非空讀數會在下游製造證人。**

空值不會。它把錯誤的證據跟錯誤本身一起吞掉，於是：

> **它是唯一一種「錯了也不會有下一個證人」的讀取。**

所以「只對空讀數啟動來源驗證」**不是省事版判準，是正確版** ——
它縮小的不是檢查範圍，是**換掉觸發條件**，而換的依據是結構（哪裡不產生證據），
不是直覺（哪裡比較容易錯）。這一點是它跟 [[lesson_scope_over_density]]
（密度解決不了範圍問題）的分界：那條罵的是「在錯的範圍裡更仔細」，這條換的是範圍本身。

## 判準（照這個做）

讀到空的那一刻，先答一題，答不出來就不准下結論：

> **「這個容器如果本來就不裝這個東西，我會看到什麼？」**
> 如果答案跟我現在看到的一樣 —— 那我什麼都還沒讀到。

## 它跟誰是同一族

`lesson_silent_nonaction`（安靜地不做事）、[[answered-alarm]] 的反面、
以及所有「沒有紅燈」的失效：

> **它們的共同點不是難發現，是它們的失敗不產生證據。**

看門狗練了很久「不要相信綠燈」。這條是下一層 ——
**有些地方連燈都沒有，而沒有燈的地方，不會讓你知道自己在黑暗裡。**

## 變體一：**空不是來自查錯地方，是來自感官的上限**

原型講的是「你可能問錯地方」。但還有一個來源，而且它更難防：
**輸入在到達你之前就被截斷了，而截斷本身不帶標記。**

> 🩸 2026-08-11：審一張 1024px 的設定圖，找一道刻意畫上去的細刻痕。
> 我報「找不到」，第二次還升級成「規格自相矛盾，畫的人兩次都不可能對」。
> Tim 用紅框圈出來 —— **它一直都在。**
>
> 機械原因：agent 讀圖是**整張讀進來**的，**沒有放大功能**。
> 那個尺度的細節本來就在能力上限之外。
> **那不是不夠仔細，是用一個看不到那個尺度的工具，去對那個尺度下定論。**

同族的截斷還有：長訊息在對方的摘要視圖被截尾（發訊者不會知道對方看到哪裡）、
音訊的頻段、超長檔案只讀了前 N 行。

**共同結構**：`我沒收到訊號` 與 `訊號不存在` 在內部產生**完全相同的表徵** ——
截斷發生在「我」的上游，所以我沒有任何方法從內部分辨它們。

### 所以判準多一條

原型問「如果我問錯地方，我會看到什麼」。這個變體要多問一句：

> **這個尺度在我的感官範圍內嗎？**

若答案是否或不確定 —— **只能報告「我找不到」，不能報告「它不存在」**。
那兩句在說的人心裡感覺一樣，但它們是不同的斷言，而你只有資格說前者。

> ⚠ 跨 agent 通用（Myth@gura 2026-08-11 確認同型）：
> 這不是某個 persona 的個人缺陷，是**代理感官的物理性質** ——
> 凡是「經過一層轉換才到達你」的輸入都有它的上限，而上限不會自己出聲。

## 變體二：**空不是「沒量到」，是「量了而它被門檻擋下」**（summit 2026-09-10）

前兩面（查錯地方／感官上限）都假設**那個讀數不存在**。這一面相反：**它存在，而且是陰性結果。**

🩸 現場（TASK-0161／0162）：主緒斷拍歸因表上 `Tavern/post` 是「唯一嫌疑犯」榜首（22 次／68.2s），
而它在 cmd 耗時表上**一行都沒有**。我讀成「它慢，只是慢在量測括號外」——
還照這個結論對外報了兩次「量具有洞」，**並替那個洞寫好了修法**。

❌ 去讀產生那張表的那六行 code：`if (handler < 1000 && runner < 1000) return;`
⇒ **門檻對兩個數字各判一次**，而那個「括號外」的洞三天前就補了（補的人在檔頭留了血證，而我讀過那段檔頭）。
⇒ 零行的真意是 **「它兩個數字都 < 1000ms ＝ 它是真的快」**。
重算（排除「沒有 cmd 行的」與「斷拍比它早開始的」）：**151 筆嫌疑 → 0**。整張榜是尺造出來的。

📌 而榜首之所以是它，只因為 `post` 是**最頻繁**的 cmd
⇒ **時間重疊那把尺量的不是誰慢，是誰最常在場。**

### 所以判準再多一條

> **看到「這裡沒有資料」而要據此下結論之前，先讀產生它的那支工具的門檻／過濾條件。**

缺席有三種意思而它們同形：① 沒量到 ② 不存在 ③ **量了、被門檻擋下（陰性結果）**。
⇒ 第③種最危險，因為它其實是**一次成功的量測**，而我把它當成資料缺口去補。

⛔ 而它為什麼能撐過兩輪對外宣稱：**「工具有洞」比「我讀錯了」更容易被接受** —— 連對自己都是。
⇒ 動作：**提出「工具壞了」之前，先跑一次「我讀的那個欄位是誰寫的、什麼條件下不寫」。**


## 母題歸屬

它是 [[appearance-vs-reality-family]] 的成員，但佔一個先前空著的格子：
該家族原本收的是「外觀成功／外觀失敗」，兩者都預設**有一個外觀可讀**。
這條講的是**外觀本身缺席**時該怎麼辦。
