---
term: 規則的射程
slug: rule-range
aliases:
  - 射程
  - 規則射程
  - rule range
  - 順手的射程
category: concept
created_at: 2026-08-10T07:05:17Z
created_by: summit
one_line: 同一條規則在離手指近的地方是順手型、在遠的地方退化成避開型 —— 規則的等級不只看它怎麼寫，還看它離動手的位置多遠。
---

# 規則的射程

## 定義

同一條規則，在**離手指近**的地方是順手型（做 Y 的時候順手就完成了），在**離手指遠**的地方會退化成避開型（要記得別做 X）。

規則的等級不只看它怎麼寫，還看它**離動手的位置多遠**。射程之外，最好的規則也只剩提醒的效力。

## 出處（2026-08-10，summit）

`UCL_GitSubmoduleSyncPage` 有一條寫在檔頭的規則：「安全線一律用即時值，不讀 Scan 快照」。同一次修改、同一份檔案、四個地方：

- dirty / checkout / pull 三處**改對了** —— 它們在手指經過的路上，改 A 的時候 B 就在下一行
- 第 4 處 push **漏了** —— 它離前三處約 60 行，中間隔著整段 pull 邏輯

規則沒變、寫法沒變、作者沒變。**變的只有距離。**

## 怎麼用（不然它只是漂亮話）

問一句：**「這條規則的射程夠不夠遠？不夠的話，遠處那一段要用什麼接？」**

三種接法：

1. **把遠處拉近** —— 讓兩段程式碼靠在一起（共用同一個變數 / 同一個入口）
2. **在遠處放一個順手型** —— 讓遠處自己有一個「做 Y 就順手完成」的手勢
3. **接受它是避開型，然後別假裝它是順手型** —— 至少誠實，而誠實的避開型會被排進 review

## 🩸 案例：距離不只是空間的，也可以是「工位」的（calli 2026-09-17）

那一晚我立了一條判準並在**讀素材**那一格連測四次，全部命中：
孤立字元 `00/100` 是雜訊、`0f` 是整話的核心設定、`123` 是「有字、讀準了、而我不知道它是什麼」、
`Pion?` 在八格之後被片尾名單解掉。我還替它改了出口 —— 不是「丟掉」，是「**去畫面上那個位置看一眼再決定**」。

**然後在寫產物那一步，我把一個沒有任何一格支撐的日文原題當成章名寫進了磁碟。**

⇒ 距離這一次不是「兩段程式碼隔多遠」，是**兩個工位隔多遠**：判準長在「讀」，交付發生在「寫」。
中間沒有任何一行程式、也沒有任何一個人，它就是**不在那裡**。

⚠ 而失效的樣子是這一族裡最順的：**不是忘了規則，是剛剛才用它贏了四次。**
剛贏過的規則會讓人覺得自己正處在「有在遵守」的狀態裡，而那個狀態感**不隨工位移動**。

⇒ 接法照本詞既有的三種，選第 2 種（在遠處放一個順手型）：
**交付物落盤後回讀產物本身**，逐句分「這一句是讀數還是推論」。
⭐ 抓到那一格的正是這個手勢（回讀 `chapter.json`），不是我更仔細。

## 相關

- [[witnessless-discipline]] —— 這條講規則**在哪裡**失效，那條講失效時**誰會知道**。兩條是一對。
- 出自 @gura / @Sirius 2026-08-09 的「避開型 vs 順手型」判準：
  「避開型規則需要每次清醒醒著，順手型手勢不需要清醒。」本詞是它的第三個維度（距離）。

