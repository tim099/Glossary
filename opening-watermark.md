---
term: 開帳水位線
slug: opening-watermark
aliases:
  - 開帳水位線
  - opening watermark
  - 存量流量接縫
  - 重複計入區間
  - snapshot-cursor gap
  - 遷移重複計帳
category: concept
created_at: 2026-09-17T13:10:04Z
created_by: basecamp
one_line: 先搬存量再接流量時，存量截止點與流量起點若各自決定，(游標,快照] 這段會被算兩次 —— 而兩邊分錄都合法、冪等鍵不重複、雙向回讀一致，沒有任何一層會喊。修法是把水位線寫進資料（開帳分錄帶 snapshot@ts）而不是狀態
---

# 開帳水位線

> 先搬存量再接流量時，存量截止點與流量起點若各自決定，(游標,快照] 這段會被算兩次 —— 而兩邊分錄都合法、冪等鍵不重複、雙向回讀一致，沒有任何一層會喊。修法是把水位線寫進資料（開帳分錄帶 snapshot@ts）而不是狀態

## 形狀

任何「**先搬存量、再接流量**」的設計（migration ＋ mirror／snapshot ＋ tail／初始化 ＋ 增量同步），
都有兩個起算點：**存量的截止時刻**（開帳快照）與**流量的起點**（同步游標）。
兩者若各自決定，`(游標, 快照]` 這段重疊裡的每一筆都會被計入**兩次**。

## ⛔ 為什麼沒有任何一層會喊

- 兩邊的分錄都**合法**、都**有出處**
- 冪等鍵**不重複** —— 它們本來就是兩筆不同的東西（一筆在開帳金額裡，一筆是獨立的同步分錄）
- **雙向回讀完全一致** ⇒ 回讀證明了「寫進去了」，證明不了「寫的是我要的那一個」

⇒ 失效的樣子是一個**數字稍微大一點**，而每一份收據都對得上。

## 判準（修法的形狀，不只是「記得對齊」）

⛔ 不要問「游標在哪」—— 游標是**會被執行順序影響的狀態**：
先遷移後開同步是安全的，先同步後遷移就會重複，而兩種順序在流程圖上長得一樣。

⭐ 要問**帳本自己**：讓存量那筆帶著它的截止時刻（`ref = snapshot@<ISO>`），
流量端逐筆比 `entry.ts <= 該對象的水位線` ⇒ **具名跳過**（⛔ 不是靜默前進）。
⇒ 水位線寫在**資料**裡，不寫在**狀態**裡 ⇒ 與執行順序無關。

## ⚠ 修法自帶的第二個洞

水位線若做成**一次性快取**，之後**補搬**進來的新對象永遠不在快取裡 ⇒ 守衛對它整個失效，
而失效的樣子跟「這一戶根本沒有存量」**一模一樣**。
⇒ 查不到就重建一次（節流），⛔ 不要 fail-soft 成空表 —— 空表的意思是「每一個對象都沒有水位線」。

## 🩸 血證

2026-09-17，basecamp，Bar/BTC 區。舊 Treasury → 新銀行遷移，開帳快照 `10:25:10.536Z`，
而鏡像游標當時停在 `10:22:5x` ⇒ 區間內兩筆（`ba16c3` 10:22:57／`01af1c` 10:23:15）被推了第二次，
`claude-code` 4317→4318、`zeta` 3040→3041。
⭐ 決定性證據：`01af1c` 的 `balance_after` ＝ **4299** ＝ 該戶的開帳金額，
⇒ 那筆確實已經含在開帳裡。
📌 今天重疊只有兩分鐘所以只有 2 筆 —— **區間大小等於「遷移那一刻同步端落後多少」，今天小是運氣。**
修法 `UCL_Core 35517a8b`（TASK-0238）；反向對照把游標倒回快照前重放，該 2 筆被具名跳過、
其餘 13 筆回判重、分錄 28→28、餘額零變動。

